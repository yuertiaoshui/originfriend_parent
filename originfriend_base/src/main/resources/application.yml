server:
  port: 9001
spring:
  application:
    name: originfriend-base
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/originfriend_base?characterEncoding=utf-8&useUnicode=true&useSSL=false
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true